import { SERVER_HOST } from "@/api/database/constants/database.constants";

export const GAME_API_PORT: number = 3001;
export const GAME_API_SERVER: string = `http://${SERVER_HOST}:${GAME_API_PORT}`;
